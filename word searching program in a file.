with open("demo.txt",'r') as file:
    data = file.read()
    # print(data)
    word = input("searching for word ? :")
    
    if word in data:
        print(f"the word {word} is present")
    else:
        print(f"not present {word}")
